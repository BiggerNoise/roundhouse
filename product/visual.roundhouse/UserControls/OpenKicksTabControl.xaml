<UserControl x:Class="visual.roundhouse.UserControls.OpenKicksTabControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:UserControls="clr-namespace:visual.roundhouse.UserControls" MinHeight="200" MinWidth="200">
    <UserControl.Resources>
        <DataTemplate x:Key="TabHeaderTemplate">
            <TextBlock Text="{Binding FileName, Mode=OneWay}" />
        </DataTemplate>
        <DataTemplate x:Key="TabContentTemplate">
            <UserControls:KickTopLevelDisplay />
        </DataTemplate>
    </UserControl.Resources>
    <TabControl Name="kicksTabControl" 
                ItemsSource="{Binding OpenKicks}" 
                ItemTemplate="{StaticResource TabHeaderTemplate}" 
                ContentTemplate="{StaticResource TabContentTemplate}" >
    </TabControl>
</UserControl>
