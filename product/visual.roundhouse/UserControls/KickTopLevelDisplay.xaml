<UserControl x:Class="visual.roundhouse.UserControls.KickTopLevelDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="File:" />
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding FileName}" />
        <Expander Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Header="Connections">
            <ListView ItemsSource="{Binding Connections}"  >
            </ListView>
        </Expander>
    </Grid>
</UserControl>
